<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
    This resource directionary contains the text and style for the help text
    that is displayed in window.
    I have put it in a separate resource file just to keep it separate from the main
    sample code.
    -->

    <FlowDocument
        x:Key="helpText"
        >
        <Paragraph>
            Left-clicking a rectangle clears the existing selection and selects the clicked rectangle (default ListBox behavior).
        </Paragraph>
        <Paragraph>
            Control + left-clicking a rectangle toggles the selection of that item (default ListBox behavior).
        </Paragraph>
        <Paragraph>
            Left-dragging a box around rectangles clears the existing selection and selects those rectangles.
        </Paragraph>
        <Paragraph>
            Control + A selects all (default ListBox behavior).
        </Paragraph>
    </FlowDocument>

</ResourceDictionary>